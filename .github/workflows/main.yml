name: JFrog CI Pipeline
on: [push]

permissions:
  id-token: write
  contents: read

env:
  JFROG_CLI_BUILD_NAME: ${{ github.repository }}-build
  JFROG_CLI_BUILD_NUMBER: ${{ github.run_number }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup JFrog CLI
        uses: jfrog/setup-jfrog-cli@v4
        env:
          JF_URL: ${{ secrets.JF_URL }}
        with:
          oidc-provider-name: github-oidc

      - name: Debug JFrog Config
        run: jf config show 2>&1 || true
        
      - name: Alias Server ID
        run: |
          SERVER_ID=$(jf config show | grep "Server ID:" | awk '{print $NF}')
          echo "Detected server ID: $SERVER_ID"
          jf c add artifactory \
            --url="${{ secrets.JF_URL }}" \
            --oidc-provider-name=github-oidc \
            --overwrite

      - name: Collect Build Metadata
        run: |
          jf rt build-collect-env
          jf rt build-add-git

      - name: Configure Maven
        run: |
          jf mvnc \
            --server-id-resolve artifactory \
            --repo-resolve-releases  libs-release \
            --repo-resolve-snapshots libs-snapshot \
            --server-id-deploy artifactory \
            --repo-deploy-releases  libs-release-local \
            --repo-deploy-snapshots libs-snapshot-local

      - name: Build, Test and Deploy
        run: jf mvn clean deploy

      - name: Docker Build and Push
        run: |
          DOMAIN=$(echo "${{ secrets.JF_URL }}" | sed 's|https\?://||' | sed 's|/$||')
          IMAGE_TAG=$DOMAIN/docker-local/spring-petclinic:${{ github.run_number }}
          docker build -t $IMAGE_TAG .
          jf docker push $IMAGE_TAG --module=docker

      - name: Publish Build Info
        run: jf rt build-publish
